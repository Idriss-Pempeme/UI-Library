import AuthForm from '@/components/templates/auth/AuthForm';

export const metadata = {
  title: 'Authentication Flow | Lumino',
  description: 'A beautiful authentication flow component',
};

export default function AuthPage() {
  return <AuthForm />;
}
